/*Dùng hàm scandir trên hệ điều hành Linux để viết chương trình sau.
Nhập vào 1 xâu ký tự là đường dẫn đến một thư mục.
Hãy đếm tổng số file của thư mục đó bao gồm cả các file nằm trong các thư mục con.
Bài này không có test case.*/

//Bài này chịu rồi không dùng Linux